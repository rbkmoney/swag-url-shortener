type: object
required:
  - sourceUrl
  - deadline
properties:
  id:
    type: string
    readOnly: true
  shortUrl:
    type: string
    format: uri
    readOnly: true
  sourceUrl:
    type: string
    format: uri
  deadline:
    type: string
    format: date-time
